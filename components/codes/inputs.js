
/* création de inputes */

leistrap.whenReady(function () {

    // recupération de tous les inputs via l'élément Input
    var Mesinputs = leistrap.Inputs();

    // maintenant créyons notre composant de boutons radio
    var MesBouttonsradio = Mesinputs.getRadio();
})

/* 
@explain
{
    "#$p#$h4la méthode .whenReady"
}
*/